# Diagnostics boundary

Future frame timing, draw-call counts, GPU capability reporting, deterministic
replay hashes, and screenshot capture hooks live here. Diagnostics must remain
removable from production builds without changing simulation behavior.

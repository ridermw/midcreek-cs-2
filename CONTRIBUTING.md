# Contributing

Use small, reviewable commits. Keep deterministic behavior covered by tests and
record visual changes with fixed-seed before/after screenshots.

Before pushing:

```powershell
npm run check
```

Do not commit private data-centre photography, credentials, generated build
output, Playwright reports, or unoptimized source assets.
